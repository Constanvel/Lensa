/** Loading is a static state: opacity down, one small caps label. */
export default function Loading() {
  return (
    <main className="page">
      <div className="loading">Loading</div>
    </main>
  );
}
